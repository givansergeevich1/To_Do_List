<Window x:Class="ToDoList.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="To-Do List" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add Task" Click="AddTask_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Delete" Click="DeleteTask_Click" Width="80"/>
        </StackPanel>

        <DataGrid x:Name="TasksGrid" Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  RowHeight="35"
                  GridLinesVisibility="All"
                  CellEditEnding="OnCellEdit"
                  MouseDoubleClick="OnTaskDoubleClick">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Created" 
                                  Binding="{Binding CreatedAt, StringFormat='dd/MM/yyyy HH:mm'}" 
                                  Width="150" IsReadOnly="True"/>
                <DataGridCheckBoxColumn Header="Done" 
                                      Binding="{Binding IsCompleted}" 
                                      Width="80"/>
                <DataGridTextColumn Header="Task" 
                                  Binding="{Binding Title}" 
                                  Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" x:Name="StatusText" Margin="0,10,0,0"/>
    </Grid>
</Window>